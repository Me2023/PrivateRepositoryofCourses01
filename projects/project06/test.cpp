#include "city.cpp"
using namespace std;
 
int main () {
    //vector<city> cityList;
    //readAQCs("data.txt", cityList); // 该txt相比最原始的，编码由ANSI改为UTF-8
    vector<int> a;
    a.push_back(1);
    cout << a.size() << endl;
    cout << a[0] << endl;
    return 0;
}